import { RecaptilatifePipe } from './recaptilatife.pipe';

describe('RecaptilatifePipe', () => {
  it('create an instance', () => {
    const pipe = new RecaptilatifePipe();
    expect(pipe).toBeTruthy();
  });
});
