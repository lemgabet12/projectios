import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.templates',
  appName: 'io.ionic.templates',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
